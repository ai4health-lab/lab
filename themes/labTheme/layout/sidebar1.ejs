<!-- 侧边栏 -->
<div class="sidebar">
    <h3><i class="fas fa-paper-plane"></i> <%= __('join.title') %></h3>
    <a href="/lab/<%= page.lang || config.language %>/join/"><%= __('join.content') %></a>
</div>

<style>
    .sidebar {
        width: 250px;
        height: 120px;
        border: 2px solid #418ac8;
        border-radius: 5px;
        background-color: #f9f9f9; /* 背景色 */
    }

    .sidebar h3 {
        background-color: #418ac8;
        color: white;
        padding: 10px;
        margin: 0; /* 移除默认的 margin */
        border-top-left-radius: 3px;
        border-top-right-radius: 3px;
    }

    .sidebar a {
        display: block;
        text-align: center;
        padding: 10px 0;
        margin: 10px;
        color: #000000;
        font-size: 16px;
        text-decoration: none; /* 移除默认的下划线 */
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    .sidebar a:hover {
        background-color: #316ba8; /* 悬停时背景色 */
    }

    .sidebar p {
        padding: 10px;
        margin: 0;
    }
</style>
